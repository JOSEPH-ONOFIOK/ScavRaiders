# Copy this to .env.local and fill in your values.

# Google Apps Script web app URL — handles sheet writes, dedup, and spot count.
# Follow the setup instructions at the top of app/api/allowlist/route.js.
GOOGLE_SCRIPT_URL=https://script.google.com/macros/s/YOUR_SCRIPT_ID/exec
